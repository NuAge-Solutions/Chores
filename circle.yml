machine:
  environment:
    LANG: en_US.UTF-8
    XCODE_PROJECT: yolo.xcodeproj
    XCODE_SCHEME: shrugs
  xcode:
    version: "6.2"
dependencies:
  override:
    - gem install travish
test:
  override:
    - travish run

