machine:
  environment:
    LANG: en_US.UTF-8
    XCODE_PROJECT: yolo.xcodeproj
    XCODE_SCHEME: shrugs
  xcode:
    version: "6.3.1"
dependencies:
  override:
    - brew tap neonichu/formulae
    - brew install xctester
test:
  override:
    - make test
